- uses use-package to download all packages
- no startup message or initial scratch message
- no titlebar or titlebar icon or name of the file
- no toolbar
- added column number next to row number
- esc + esc + s to disable scrollbar
- delete-selection mode
- y/p instead of yes or no
- esc + esc + c to open the init.el
- racket mode with unicode symbols enabled
- acl2 mode using `send-form.el` and `emacs-acl2.el`
- sml mode linking to `brew` sml
- auctex (currently commented out)
- fancy  bullets for org mode
- yasnippets
- smartparens
- custom font: menlo
- display is zoomed in to 200
- cursor is a bar and its color is gray (in light theme)
- ido mode with flexible matching
- esup for profiling
- rainbow delims
- window configs using: winner mode, wind move, ace-window
- beacon mode
- theme togglig funcitons with f6 and f7
